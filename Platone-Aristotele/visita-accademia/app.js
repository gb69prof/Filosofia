const viewport=document.querySelector('#mapViewport'),stage=document.querySelector('#mapStage'),dialog=document.querySelector('#infoDialog'),toast=document.querySelector('#toast');let scale=1,timer;
const progress=JSON.parse(localStorage.getItem('pa-visit-progress')||'{}');
function say(text){clearTimeout(timer);toast.textContent=text;toast.classList.add('show');timer=setTimeout(()=>toast.classList.remove('show'),2600)}
function setScale(next){scale=Math.max(.45,Math.min(1.25,next));stage.style.transform=`scale(${scale})`;stage.style.marginBottom=`${1024*(scale-1)}px`;stage.style.marginRight=`${1536*(scale-1)}px`}
function fit(){const next=Math.min(viewport.clientWidth/1536,(viewport.clientHeight||900)/1024,1);setScale(Math.max(.45,next));viewport.scrollTo({left:0,top:0,behavior:'smooth'})}
function updateProgress(){const completed=Object.values(progress).filter(Boolean).length;document.querySelector('#summaryValue').textContent=`${completed} / 14`;if(progress.p1)document.querySelector('.p1').classList.add('completed')}
function openDialog(eye,title,text,items=[]){document.querySelector('#dialogEye').textContent=eye;document.querySelector('#dialogTitle').textContent=title;document.querySelector('#dialogText').textContent=text;document.querySelector('#dialogList').innerHTML=items.map(x=>`<div>${x}</div>`).join('');dialog.showModal()}
document.querySelector('#zoomIn').addEventListener('click',()=>setScale(scale+.1));document.querySelector('#zoomOut').addEventListener('click',()=>setScale(scale-.1));document.querySelector('#fitMap').addEventListener('click',fit);document.querySelectorAll('[data-locked]').forEach(button=>button.addEventListener('click',()=>say(`${button.dataset.locked}: la tappa sarà costruita nel prossimo passaggio.`)));document.querySelectorAll('[data-info]').forEach(button=>button.addEventListener('click',()=>{if(button.dataset.info==='neo')openDialog('La via della contemplazione','Il platonismo continua','Questa diramazione accennerà alla lunga trasformazione del platonismo senza diventare un secondo manuale.',['Speusippo e Senocrate','Accademia media e nuova','Medioplatonismo','Plotino, Porfirio e Proclo','Agostino e il cristianesimo']);else openDialog('E oggi?','Le domande arrivano fino a noi','Una terza strada collegherà i problemi antichi al presente.',['Intelligenza artificiale','Democrazia e potere','Scienza e ambiente','Tecnologia e umanità','Etica e responsabilità','Linguaggio e comunicazione'])}));document.querySelector('#helpBtn').addEventListener('click',()=>openDialog('Come funziona','Un luogo, non un indice','Ogni tappa apre una scena esplorabile. I personaggi, gli oggetti e i problemi conducono alle sezioni già costruite della PWA. Completa una tappa per illuminare il sentiero.'));document.querySelector('#progressBtn').addEventListener('click',()=>openDialog('Le mie tappe','Il cammino percorso',progress.p1?'Hai completato Il Giardino di Socrate. Le altre tappe saranno aperte progressivamente.':'Non hai ancora completato tappe. Inizia dal Giardino di Socrate.'));document.querySelectorAll('.dialogClose').forEach(b=>b.addEventListener('click',()=>dialog.close()));addEventListener('resize',()=>{if(innerWidth<760)fit()});setTimeout(fit,80);updateProgress();if('serviceWorker'in navigator)addEventListener('load',()=>navigator.serviceWorker.register('../sw.js',{updateViaCache:'none'}));